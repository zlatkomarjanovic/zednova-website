import { Syne } from "next/font/google";

/** Logo wordmark — Syne reads sharper and more ownable than body sans. */
export const brandWordmark = Syne({
  subsets: ["latin"],
  weight: ["700"],
  display: "swap",
});
